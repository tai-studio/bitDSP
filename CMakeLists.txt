####################################################################################################
# CMakeLists file for bitDSP
# Generated by LFSaw
# 2020-03-11
####################################################################################################

####################################################################################################
# basic project config
cmake_minimum_required(VERSION 3.5)
set(project_name "bitDSP")
set(CMAKE_MODULE_PATH ${CMAKE_SOURCE_DIR}/cmake_modules ${CMAKE_MODULE_PATH})
set(CMAKE_CXX_STANDARD 11)

####################################################################################################
# load modules
include(SuperColliderServerPlugin RESULT_VARIABLE server_plugin_found)
if(NOT server_plugin_found)
    message(FATAL_ERROR "Could not find server plugin functions module")
endif()

include(SuperColliderCompilerConfig RESULT_VARIABLE compiler_config_found)
if(NOT compiler_config_found)
    message(FATAL_ERROR "Could not find compiler config module")
endif()

# Windows - puts redistributable DLLs in install directory
include(InstallRequiredSystemLibraries)

sc_check_sc_path("${SC_PATH}")
message(STATUS "Found SuperCollider: ${SC_PATH}")
set(SC_PATH "${SC_PATH}" CACHE PATH
    "Path to SuperCollider source. Relative paths are treated as relative to this script" FORCE)

include("${SC_PATH}/SCVersion.txt")
set(SC_VERSION "${PROJECT_VERSION_MAJOR}.${PROJECT_VERSION_MINOR}${PROJECT_VERSION_PATCH}")
message(STATUS "Building plugins for SuperCollider version: ${SC_VERSION}")

# set project here to avoid SCVersion.txt clobbering our version info
project(${project_name})
sc_do_initial_compiler_config() # do after setting project so compiler ID is available

if(CMAKE_INSTALL_PREFIX_INITIALIZED_TO_DEFAULT OR NOT CMAKE_INSTALL_PREFIX)
    message(WARNING "No install prefix provided, defaulting to $BUILD_DIR/install")
    set(CMAKE_INSTALL_PREFIX "${CMAKE_BINARY_DIR}/install" CACHE PATH "Install prefix" FORCE)
endif()

message(STATUS "Install directory set to: ${CMAKE_INSTALL_PREFIX}")

####################################################################################################
# options
option(SUPERNOVA "Build plugins for supernova" ON)
option(SCSYNTH "Build plugins for scsynth" ON)
option(NATIVE "Optimize for native architecture" OFF)
option(STRICT "Use strict warning flags" OFF)

####################################################################################################
# Begin target BitAnd

set(BitAnd_cpp_files
    plugins/BitAnd.cpp
    plugins/BitAnd.hpp
)
set(BitAnd_sc_files
    classes/BitAnd.sc
)
set(BitAnd_schelp_files
    help/BitAnd.schelp
)

sc_add_server_plugin(
    "bitDSP/BitAnd" # desination directory
    "BitAnd" # target name
    "${BitAnd_cpp_files}"
    "${BitAnd_sc_files}"
    "${BitAnd_schelp_files}"
)

# End target BitAnd
####################################################################################################

####################################################################################################
# Begin target BitDAC

set(BitDAC_cpp_files
    plugins/BitDAC.cpp
    plugins/BitDAC.hpp
)
set(BitDAC_sc_files
    classes/BitDAC.sc
)
set(BitDAC_schelp_files
    help/BitDAC.schelp
)

sc_add_server_plugin(
    "bitDSP/BitDAC" # desination directory
    "BitDAC" # target name
    "${BitDAC_cpp_files}"
    "${BitDAC_sc_files}"
    "${BitDAC_schelp_files}"
)

# End target BitDAC
####################################################################################################

####################################################################################################
# Begin target BitNot

set(BitNot_cpp_files
    plugins/BitNot.cpp
    plugins/BitNot.hpp
)
set(BitNot_sc_files
    classes/BitNot.sc
)
set(BitNot_schelp_files
    help/BitNot.schelp
)

sc_add_server_plugin(
    "bitDSP/BitNot" # desination directory
    "BitNot" # target name
    "${BitNot_cpp_files}"
    "${BitNot_sc_files}"
    "${BitNot_schelp_files}"
)

# End target BitNot
####################################################################################################

####################################################################################################
# Begin target BitOr

set(BitOr_cpp_files
    plugins/BitOr.cpp
    plugins/BitOr.hpp
)
set(BitOr_sc_files
    classes/BitOr.sc
)
set(BitOr_schelp_files
)

sc_add_server_plugin(
    "bitDSP/BitOr" # desination directory
    "BitOr" # target name
    "${BitOr_cpp_files}"
    "${BitOr_sc_files}"
    "${BitOr_schelp_files}"
)

# End target BitOr
####################################################################################################

####################################################################################################
# Begin target BitPrint

set(BitPrint_cpp_files
    plugins/BitPrint.cpp
    plugins/BitPrint.hpp
)
set(BitPrint_sc_files
    classes/BitPrint.sc
)
set(BitPrint_schelp_files
)

sc_add_server_plugin(
    "bitDSP/BitPrint" # desination directory
    "BitPrint" # target name
    "${BitPrint_cpp_files}"
    "${BitPrint_sc_files}"
    "${BitPrint_schelp_files}"
)

# End target BitPrint
####################################################################################################

####################################################################################################
# Begin target BitScramble

set(BitScramble_cpp_files
    plugins/BitScramble.cpp
    plugins/BitScramble.hpp
)
set(BitScramble_sc_files
    classes/BitScramble.sc
)
set(BitScramble_schelp_files
)

sc_add_server_plugin(
    "bitDSP/BitScramble" # desination directory
    "BitScramble" # target name
    "${BitScramble_cpp_files}"
    "${BitScramble_sc_files}"
    "${BitScramble_schelp_files}"
)

# End target BitScramble
####################################################################################################

####################################################################################################
# Begin target BitSort

set(BitSort_cpp_files
    plugins/BitSort.cpp
    plugins/BitSort.hpp
)
set(BitSort_sc_files
    classes/BitSort.sc
)
set(BitSort_schelp_files
)

sc_add_server_plugin(
    "bitDSP/BitSort" # desination directory
    "BitSort" # target name
    "${BitSort_cpp_files}"
    "${BitSort_sc_files}"
    "${BitSort_schelp_files}"
)

# End target BitSort
####################################################################################################

####################################################################################################
# Begin target BitVal

set(BitVal_cpp_files
    plugins/BitVal.cpp
    plugins/BitVal.hpp
)
set(BitVal_sc_files
    classes/BitVal.sc
)
set(BitVal_schelp_files
)

sc_add_server_plugin(
    "bitDSP/BitVal" # desination directory
    "BitVal" # target name
    "${BitVal_cpp_files}"
    "${BitVal_sc_files}"
    "${BitVal_schelp_files}"
)

# End target BitVal
####################################################################################################

####################################################################################################
# Begin target BitXor

set(BitXor_cpp_files
    plugins/BitXor.cpp
    plugins/BitXor.hpp
)
set(BitXor_sc_files
    classes/BitXor.sc
)
set(BitXor_schelp_files
)

sc_add_server_plugin(
    "bitDSP/BitXor" # desination directory
    "BitXor" # target name
    "${BitXor_cpp_files}"
    "${BitXor_sc_files}"
    "${BitXor_schelp_files}"
)

# End target BitXor
####################################################################################################

####################################################################################################
# Begin target DBitAnd

set(DBitAnd_cpp_files
    plugins/DBitAnd.cpp
)
set(DBitAnd_sc_files
    classes/DBitAnd.sc
)
set(DBitAnd_schelp_files
)

sc_add_server_plugin(
    "bitDSP/DBitAnd" # desination directory
    "DBitAnd" # target name
    "${DBitAnd_cpp_files}"
    "${DBitAnd_sc_files}"
    "${DBitAnd_schelp_files}"
)

# End target DBitAnd
####################################################################################################

####################################################################################################
# Begin target DBitLFSR

set(DBitLFSR_cpp_files
    plugins/DBitLFSR.cpp
)
set(DBitLFSR_sc_files
    classes/DBitLFSR.sc
)
set(DBitLFSR_schelp_files
    help/DBitLFSR.schelp
)

sc_add_server_plugin(
    "bitDSP/DBitLFSR" # desination directory
    "DBitLFSR" # target name
    "${DBitLFSR_cpp_files}"
    "${DBitLFSR_sc_files}"
    "${DBitLFSR_schelp_files}"
)

# End target DBitLFSR
####################################################################################################

####################################################################################################
# Begin target DBitNot

set(DBitNot_cpp_files
    plugins/DBitNot.cpp
)
set(DBitNot_sc_files
    classes/DBitNot.sc
)
set(DBitNot_schelp_files
)

sc_add_server_plugin(
    "bitDSP/DBitNot" # desination directory
    "DBitNot" # target name
    "${DBitNot_cpp_files}"
    "${DBitNot_sc_files}"
    "${DBitNot_schelp_files}"
)

# End target DBitNot
####################################################################################################

####################################################################################################
# Begin target DBitOr

set(DBitOr_cpp_files
    plugins/DBitOr.cpp
)
set(DBitOr_sc_files
    classes/DBitOr.sc
)
set(DBitOr_schelp_files
)

sc_add_server_plugin(
    "bitDSP/DBitOr" # desination directory
    "DBitOr" # target name
    "${DBitOr_cpp_files}"
    "${DBitOr_sc_files}"
    "${DBitOr_schelp_files}"
)

# End target DBitOr
####################################################################################################

####################################################################################################
# Begin target DBitScramble

set(DBitScramble_cpp_files
    plugins/DBitScramble.cpp
)
set(DBitScramble_sc_files
    classes/DBitScramble.sc
)
set(DBitScramble_schelp_files
)

sc_add_server_plugin(
    "bitDSP/DBitScramble" # desination directory
    "DBitScramble" # target name
    "${DBitScramble_cpp_files}"
    "${DBitScramble_sc_files}"
    "${DBitScramble_schelp_files}"
)

# End target DBitScramble
####################################################################################################

####################################################################################################
# Begin target DBitShifter

set(DBitShifter_cpp_files
    plugins/DBitShifter.cpp
)
set(DBitShifter_sc_files
    classes/DBitShifter.sc
)
set(DBitShifter_schelp_files
    help/DBitShifter.schelp
)

sc_add_server_plugin(
    "bitDSP/DBitShifter" # desination directory
    "DBitShifter" # target name
    "${DBitShifter_cpp_files}"
    "${DBitShifter_sc_files}"
    "${DBitShifter_schelp_files}"
)

# End target DBitShifter
####################################################################################################

####################################################################################################
# Begin target DBitSort

set(DBitSort_cpp_files
    plugins/DBitSort.cpp
)
set(DBitSort_sc_files
    classes/DBitSort.sc
)
set(DBitSort_schelp_files
)

sc_add_server_plugin(
    "bitDSP/DBitSort" # desination directory
    "DBitSort" # target name
    "${DBitSort_cpp_files}"
    "${DBitSort_sc_files}"
    "${DBitSort_schelp_files}"
)

# End target DBitSort
####################################################################################################

####################################################################################################
# Begin target DBitVal

set(DBitVal_cpp_files
    plugins/DBitVal.cpp
)
set(DBitVal_sc_files
    classes/DBitVal.sc
)
set(DBitVal_schelp_files
)

sc_add_server_plugin(
    "bitDSP/DBitVal" # desination directory
    "DBitVal" # target name
    "${DBitVal_cpp_files}"
    "${DBitVal_sc_files}"
    "${DBitVal_schelp_files}"
)

# End target DBitVal
####################################################################################################

####################################################################################################
# Begin target DBitXor

set(DBitXor_cpp_files
    plugins/DBitXor.cpp
)
set(DBitXor_sc_files
    classes/DBitXor.sc
)
set(DBitXor_schelp_files
)

sc_add_server_plugin(
    "bitDSP/DBitXor" # desination directory
    "DBitXor" # target name
    "${DBitXor_cpp_files}"
    "${DBitXor_sc_files}"
    "${DBitXor_schelp_files}"
)

# End target DBitXor
####################################################################################################

####################################################################################################
# END PLUGIN TARGET DEFINITION
####################################################################################################

message(STATUS "Generating plugin targets done")
